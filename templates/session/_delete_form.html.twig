<form method="post" action="{{ path('session_delete', {'id': session.id}) }}" onsubmit="return confirm('Jesteś pewny, że chcesz usunąć ten element?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session.id) }}">
    <button class="btn">Usuń</button>
</form>
