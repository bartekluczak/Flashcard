{{ form_start(form) }}
    <div style="width: 300px; margin-left: 5px; margin-bottom: 10px;">
        <h6>Wybierz grupę do nauki</h6>
        {{ form_widget(form.groupId, { 'attr': {'class': 'form-control'} }) }}
    </div>
    <button class="btn btn-primary" style="width: 300px; margin: 5px;">{{ button_label|default('Zapisz') }}</button>
{{ form_end(form) }}
