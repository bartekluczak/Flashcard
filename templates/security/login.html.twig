{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <form method="post">
            {% if error %}
            <div class="col-lg-6 col-md-6 col-sm-12 mx-auto">
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            </div>
            {% endif %}

            {% if app.user %}
            <div class="mb-3">
                Jesteś zalogowany jako {{ app.user.login }}, <a href="{{ path('app_logout') }}">Wyloguj</a>
                Lub przejdź do <a href='/group'> nauki</a>
            </div>
            {% endif %}
            {% if not app.user %}
            
            <h1 class="row justify-content-center h3 mb-3 mt-5 font-weight-normal">Zaloguj się</h1>
        
            <div class="row justify-content-center">
                <div class="col-lg-3 col-md-6 col-sm-9">
                    <label class="mb-1" for="inputEmail">e-mail</label>
                    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control mb-2" autocomplete="email" required autofocus>
                    <label class="mb-1" for="inputPassword">hasło</label>
                    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
                    <input type="hidden" name="_csrf_token"
                    value="{{ csrf_token('authenticate') }}"
                    >
                </div>
            </div>
            <div class="row justify-content-center">
                    <div class="col mt-3 d-grid gap-2 col-lg-3 col-md-6 col-sm-9">
                    <button class="btn btn-primary" type="submit">Zaloguj</button>
                    <a href="/register" class="btn btn-primary"> Rejestracja</a>
                    </div>
            </div>
            {% endif %}
    </form>
{% endblock %}
  
    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}
          